# AI Appointment Scheduler
This is a local AI-powered appointment scheduling app using Ollama and Python.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Ensure Ollama is running locally with the `gemma4` model.
3. Run the app: `python main.py`
